/**
 * `@aia/publishing` — emplacement réservé.
 *
 * Aucune ligne de code à l'étape 1 : la publication manuelle arrive à l'étape 5
 * et les connecteurs d'API à l'étape 9. Le contrat `PlatformConnector`
 * (docs/02 §9.2) est figé, son implémentation ne l'est pas encore.
 */

export {};
